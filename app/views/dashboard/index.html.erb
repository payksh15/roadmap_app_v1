<%= link_to t('.audios', :default => t("helpers.links.audios")), audios_path, :class => 'btn btn-primary' %>

<%= link_to t('.videos', :default => t("helpers.links.videos")), videos_path, :class => 'btn btn-primary' %>